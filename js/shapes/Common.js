/**
* SVG Shape Components
*/

import PropTypes from "prop-types";

const numberProp = PropTypes.oneOfType([PropTypes.string, PropTypes.number]);
const cloudNumber = 5;

export { numberProp, cloudNumber };
